# Left in the repository folder by older versions, which logged and staged
# there; the log records the full path of every converted file
conversion_log.txt
conversion_log.old.txt
_staging/

__pycache__/
*.pyc

# The environment "uv sync" creates
.venv/

# coverage run's data
.coverage

# PyInstaller output (packaging/topdf.spec)
build/
dist/
